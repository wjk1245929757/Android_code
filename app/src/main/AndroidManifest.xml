<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wjk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity"/>
        <activity android:name=".standard_mode" android:launchMode="standard"/>
        <activity android:name=".single_top" android:launchMode="singleTop"/>
        <activity android:name=".single_task" android:launchMode="singleTask"/>
        <activity android:name=".single_instance" android:launchMode="singleInstance"/>
        <activity android:name=".standard_mode2" android:launchMode="standard"/>
        <activity android:name=".single_top2" android:launchMode="singleTop"/>
        <activity android:name=".single_task2" android:launchMode="singleTask"/>
        <activity android:name=".single_instance2" android:launchMode="singleInstance"/>
    </application>

</manifest>